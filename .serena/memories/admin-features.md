Admin pages: AdminStats shows 8 stat cards in 2 rows (totalUsers, newUsersToday, contributors excl pending, newContributorsToday, publishedRecipes, pendingRecipes, dailyViews, dailyActiveUsers) + recent activity feed (last 5) + system health indicator; refreshes every 30s and listens to statsUpdated/recipeUpdated/userUpdated events. AdminRecipes: tabs for pending/published/rejected; RecipeTable per tab shows id, image thumbnail, title, author name, categories (via normalizeCategories), status badge, date, actions; pending tab has approve/reject buttons, all tabs have preview/delete; preview modal (max-w-3xl) shows image, description, categories+difficulty badges, ingredients list, instructions list, approve/reject buttons for pending; logs admin actions via activity (approved/rejected/removed); dispatches recipeUpdated/statsUpdated. UserList: search by username/email + role filter (all/user/admin); table shows avatar+name, email, role badge, display status badge, joined date, last active; display status logic: suspended/pending shown as-is, inactive shown as-is, otherwise active if lastActive within 5min else inactive; refreshes status display every 30s; actions limited to 3 icon buttons: approve (ShieldCheck, sets status active, DISABLED when status is active or inactive), suspend (Ban, sets status suspended, DISABLED when already suspended), delete (Trash2, with confirmation modal); admin CANNOT edit any user details (name, email, profile info, etc.) — only status changes and deletion; logs admin actions and dispatches userUpdated/statsUpdated.